# Example environment variables for Molt.bot + SSH
# DO NOT commit this file with real credentials!

# Setup wizard password
SETUP_PASSWORD=change-me-to-something-secure

# SSH Access
SSH_USERNAME=archuser
SSH_PASSWORD=change-this-password
AUTHORIZED_KEYS=
DISABLE_PASSWORD_AUTH=false

# Molt.bot configuration
MOLTBOT_STATE_DIR=/data/.moltbot
MOLTBOT_WORKSPACE_DIR=/data/workspace
MOLTBOT_GATEWAY_TOKEN=

# Gateway bind mode (security sensitive!)
# loopback (default) - Only accessible via Railway's public URL (proxied through wrapper)
# tailnet - Accessible via Tailscale network (requires Tailscale setup)
# 0.0.0.0 - Accessible from any network interface (SECURITY RISK - use with caution!)
GATEWAY_BIND=loopback

# Provider API Keys (set via /setup wizard)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=

# Telegram Bot Token (optional)
# TELEGRAM_BOT_TOKEN=

# Discord Bot Token (optional)
# DISCORD_BOT_TOKEN=
